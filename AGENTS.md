## Kỹ năng dành cho trợ lý

### Nơi quản lý công việc

Đặc tả và hạng mục được lưu cục bộ trong `.scratch/`. Xem `docs/agents/issue-tracker.md`.

### Nhãn trạng thái

Sử dụng bộ nhãn mặc định. Xem `docs/agents/triage-labels.md`.

### Tài liệu miền

Project dùng một ngữ cảnh chung tại `CONTEXT.md` và các quyết định kiến trúc tại `docs/adr/`. Xem `docs/agents/domain.md`.

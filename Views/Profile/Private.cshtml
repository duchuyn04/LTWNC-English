@model PublicProfileViewModel
@{
    // FILE: Placeholder khi hồ sơ private — người khác không xem được nội dung.
    // Model: PublicProfileViewModel tối thiểu; không lộ stats/timeline.
    // Chủ sở hữu vẫn xem Public/Edit khi đăng nhập đúng user.
    ViewData["Title"] = "Hồ sơ riêng tư";
}
@section Styles {
    <link rel="stylesheet" href="~/css/profile.css" asp-append-version="true" />
}
<div class="profile-private profile-card">
    <div class="profile-avatar">@Model.AvatarInitial</div>
    <p class="profile-eyebrow">HỒ SƠ RIÊNG TƯ</p>
    <h1>Hồ sơ đang ở chế độ riêng tư</h1>
    <p>Tài khoản này chưa bật chế độ công khai.</p>
    <a class="profile-button profile-button-dark" href="/">Về trang chủ</a>
</div>

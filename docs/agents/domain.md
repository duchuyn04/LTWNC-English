# Domain Docs

Before exploring the codebase, read:

- `CONTEXT.md` when it exists.
- Relevant ADRs under `docs/adr/`.

This repository uses a single domain context.

Create domain files lazily through `/domain-modeling`; do not create empty placeholders.

Use terminology defined in `CONTEXT.md`. Explicitly flag proposals that contradict an existing ADR.

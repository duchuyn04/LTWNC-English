{
  "name": "ltwnc-lessons-smoke",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright smoke for Lessons + practice",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
